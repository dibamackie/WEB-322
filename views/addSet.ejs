<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New LEGO Set</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar', { page: '/lego/addSet' }) %>
    <main class="container mx-auto p-4">
        <section class="bg-gray-200 p-8 text-center">
            <h1 class="text-5xl font-bold">Add Set</h1>
            <p class="py-6">Enter details of the new LEGO set you wish to add to the collection.</p>
        </section>
        <form action="/lego/addSet" method="POST" class="max-w-4xl mx-auto p-8 bg-white rounded shadow">
            <div class="mb-4">
                <label for="name" class="block text-lg font-medium text-gray-700">Name</label>
                <input type="text" id="name" name="name" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
            </div>
            <div class="mb-4">
                <label for="year" class="block text-lg font-medium text-gray-700">Year</label>
                <input type="number" id="year" name="year" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
            </div>
            <div class="mb-4">
                <label for="num_parts" class="block text-lg font-medium text-gray-700">Number of Parts</label>
                <input type="number" id="num_parts" name="num_parts" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
            </div>
            <div class="mb-4">
                <label for="img_url" class="block text-lg font-medium text-gray-700">Image URL</label>
                <input type="url" id="img_url" name="img_url" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
            </div>
            <div class="mb-4">
                <label for="theme_id" class="block text-lg font-medium text-gray-700">Theme</label>
                <select id="theme_id" name="theme_id" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>"><%= theme.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="mb-4">
                <label for="set_num" class="block text-lg font-medium text-gray-700">Set Number</label>
                <input type="text" id="set_num" name="set_num" required class="mt-1 block w-full p-2 border border-gray-300 rounded">
            </div>
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                Add Set
            </button>
        </form>
    </main>
</body>
</html>
